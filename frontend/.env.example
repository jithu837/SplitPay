# Base URL of the SplitPay backend API.
# In dev, Vite proxies /api to http://localhost:8080 (see vite.config.js),
# so this can usually stay as /api. Override for a deployed backend.
VITE_API_BASE_URL=/api
