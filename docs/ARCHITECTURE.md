# Architecture notes

All architecture diagrams (system architecture, ER diagram, auth sequence,
minimum-cash-flow flowchart, Razorpay payment sequence) live in the main
[README.md](../README.md) as Mermaid diagrams, alongside the explanations of
each flow. This file just points there rather than duplicating content that
would drift out of sync.

If you're viewing this on GitHub, the Mermaid blocks in the README render
automatically. If you're viewing it elsewhere, paste the relevant ```mermaid
block into https://mermaid.live to render it.
